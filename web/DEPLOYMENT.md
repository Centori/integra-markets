# Web deployment

www.integramarkets.app builds from this directory.

Vercel project: integra-web (rootDirectory: web, framework: nextjs).
Production branch: main — every merge here auto-deploys www.

Do NOT run 'vercel deploy' from this folder without linking first;
an unlinked deploy silently creates a stray project.
