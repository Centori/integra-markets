// Import the complete app from the app directory
import React from 'react';
import { registerRootComponent } from 'expo';
import App from './app/app/(tabs)/index';

// Register the main component
registerRootComponent(App);
